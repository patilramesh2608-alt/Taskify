[pytest]
minversion = 6.0
addopts = -q
python_files = tests.py test_*.py *_test.py

# Suppress deprecation warnings from SQLAlchemy and other libs during tests.
filterwarnings =
    ignore::DeprecationWarning
    ignore::PendingDeprecationWarning